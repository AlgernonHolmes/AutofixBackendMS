server:
  port: 8881
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/AlgernonHolmes/AutofixBackendMS
          search-paths: config-data
  application:
    name: config-server